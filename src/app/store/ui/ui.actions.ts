import { createAction } from '@ngrx/store';

export const toggleNewInvoiceModal = createAction(
  '[UI] Toggle NewInvoiceModal'
);
export const toggleConfirmDeleteModal = createAction(
  '[UI]  ConfirmDeleteModal'
);
